/*
* This command creates a schema in the database to work as a backup for the database (local mirror or replication).
*/
CREATE SCHEMA IF NOT EXISTS databasebackup AUTHORIZATION lawyercenter_aka_ifood_advogado;